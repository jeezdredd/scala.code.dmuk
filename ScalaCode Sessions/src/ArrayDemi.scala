import scala.io.StdIn._

object ArrayDemi {
  def main(args: Array[String]): Unit = {

    print("Enter Array size: ")
    var size: Int = readInt()
    var i = 0
    var myArray = new Array[Int](size)

    print(f"Enter the $size values: ")

    while (myArray.indexOf(0) i + 1) {
      

    }
  }
}
